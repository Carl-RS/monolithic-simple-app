# monolithic-simple-app
A simple web app used for testing github actions with playwright testing

## Playwright report
After the GitHub Actions workflow runs successfully on the main branch, the generated Playwright HTML report is published to GitHub Pages.

Open the report at:
https://<your-github-username>.github.io/<your-repository-name>/index.html
